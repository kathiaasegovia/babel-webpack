import React from 'react';
import ReactDOM from 'react-dom';
import './styles/styles.scss'

const App = ()=> {
  return <h1>React app setup with Babel, Webpack & Sass!</h1>
}

ReactDOM.render(<App />, document.getElementById('root'))